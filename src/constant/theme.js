import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    yellowPoint: {
      main: '#FFBD00',
      contrastText: '#fff',
    },
    black: {
      main: '#292929',
      contrastText: '#fff',
    },
  },
});

export default theme;
